@model PBManager.Dto.ViewModels.SubcategoryViewModel
@{
    ViewBag.Title = "Edit subcategory";

    SelectList categoriesComboBox = new SelectList(ViewBag.CATEGORIES, "Id", "Name", "Type", new { });
}
<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            @ViewBag.Title
            <small>Edit an existing subcategory</small>
        </h1>
        <ol class="breadcrumb">
            <li><a href=""><i class="fa fa-dashboard"></i> Home</a></li>
            <li><a href="@Url.Action("Index", "Subcategories")">Subcategories</a></li>
            <li class="active">@ViewBag.Title</li>
        </ol>
    </section>
    <!-- Main content -->
    <section class="content">
        <!-- box -->
        <div class="box box-success">
            <div class="box-body">
                <div class="row">
                    <div class="col-md-6">
                        <form role="form" action="@Url.Action("Edit", "Subcategories")" method="post">
                            @* <span class="text-red">@Html.ValidationSummary()</span> *@
                            <div class="form-group">
                                @Html.LabelFor(m => m.Name)
                                @Html.TextBoxFor(m => m.Name, new { Class = "form-control" })
                                @Html.ValidationMessageFor(m => m.Name, "", new { Class = "text-red" })
                            </div>
                            <div class="row">
                                <div class="col-md-4">
                                    <div class="form-group">
                                        @Html.LabelFor(m => m.CategoryId)
                                        @Html.DropDownListFor(m => m.CategoryId, categoriesComboBox, "(Choose one)", new { Class = "form-control" })
                                        <br>@Html.ValidationMessageFor(m => m.CategoryId, "", new { Class = "text-red" })
                                    </div>
                                </div>
                            </div>

                            @Html.HiddenFor(m => m.Id)
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn btn-primary btn-flat pull-left"><i class="fa fa-floppy-o"></i> Update</button>
                        </form>
                    </div>
                </div>
            </div>
            <!-- /.box-body -->
        </div>
        <!-- /.box -->
    </section>
    <!-- /.content -->
</div>
<!-- /.content-wrapper -->
